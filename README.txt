Purpose: To create a binary search tree, add to it, find values in it, print the values of the tree, find the min and max values of the tree, save the tree to a file, delete strings in the tree.

This program has segmentations faults and is incomplete.
